# 用户指令记忆

本文件记录了用户的指令、偏好和教导，用于在未来的交互中提供参考。

## 格式

### 用户指令条目
用户指令条目应遵循以下格式：

[用户指令摘要]
- Date: [YYYY-MM-DD]
- Context: [提及的场景或时间]
- Instructions:
  - [用户教导或指示的内容，逐行描述]

### 项目知识条目
Agent 在任务执行过程中发现的条目应遵循以下格式：

[项目知识摘要]
- Date: [YYYY-MM-DD]
- Context: Agent 在执行 [具体任务描述] 时发现
- Category: [代码结构|代码模式|代码生成|构建方法|测试方法|依赖关系|环境配置]
- Instructions:
  - [具体的知识点，逐行描述]

## 去重策略
- 添加新条目前，检查是否存在相似或相同的指令
- 若发现重复，跳过新条目或与已有条目合并
- 合并时，更新上下文或日期信息
- 这有助于避免冗余条目，保持记忆文件整洁

## 条目

[按上述格式记录的记忆条目]

[项目启动方式]
- Date: 2026-05-13
- Context: Agent 在执行杭州麻将 Web MVP 开发与预览时发现
- Category: 构建方法
- Instructions:
  - 项目是零依赖 Node.js Web 应用，使用 `npm run dev` 或 `npm start` 启动。
  - 开发服务器默认监听 `5173` 端口。

[任务执行顺序偏好]
- Date: 2026-05-13
- Context: 用户要求按任务清单连续推进当前项目开发
- Instructions:
  - 按开发任务顺序逐项执行，完成一个任务后再检查并进入下一个任务。
  - 所有开发任务完成后，再进行一轮统一 review。

[测试方法]
- Date: 2026-05-15
- Context: Agent 在执行杭州麻将规则引擎重构与状态流转测试时发现
- Category: 测试方法
- Instructions:
  - 项目使用 Node.js 内置 `node:test` 作为当前服务端规则测试方案。
  - 运行全量测试使用 `npm test`。
  - 当前规则测试覆盖状态流转、番型翻数、庄家倍率、杠后补牌和流局边界。
  - 当前规则测试还覆盖财飘链成立/中断、杠后补牌触发财飘、副露来源牌和来源座位记录。
  - 当前 AI 测试覆盖财神保留、AI 杠牌响应、财飘等待时放弃吃牌、合法响应执行。
  - 当前前端测试覆盖二次确认出牌、财飘提示、可胡提示、新摸牌高亮和副露来源展示。

[上线版迭代执行方式]
- Date: 2026-05-18
- Context: 用户要求从正式可上线目标出发持续迭代当前项目
- Instructions:
  - 后续开发以正式上线为目标推进，不再以 MVP 完成度作为边界。
  - 需要先制定详细开发计划并拆分成带完成标识的任务列表。
  - 每完成一个任务，都要在任务列表中勾选完成状态。
  - 每次完成一个任务后，都要检查是否还有下一个未完成任务；如果有，则继续执行下一个任务，如此循环推进。
